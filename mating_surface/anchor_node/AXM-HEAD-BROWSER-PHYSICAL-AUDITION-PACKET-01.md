# AXM HEAD Browser Physical-Audition Packet 01

## Object and source law

This transaction constructs one supplier-neutral two-seat browser physical-audition packet above the admitted browser observation membrane. The packet is a source-qualified compiler, external run-kit builder, private evidence assembler, independent verifier, measured-verifier bootstrap, and body-free public projection surface. It is not a supplier adapter, inference runtime, browser automation system, or admission receipt for a physical Estate.

The controlling predecessor is the admitted observation membrane at commit `e32cf641cddd00ab1c97d7d6af1708c84ea491b6`, tree `271956e4a5068c0d71f5223b032cd2e19d4a7c8d`, with candidate source commit `351f1f1e54e4d454137b69ad64e571590db134dd`. The inherited commodity interface remains `axm/distributed-model-inference@1`. Its exact admitted probe bytes, profile, materializer, direct verifier, and bootstrap are copied into each generated run kit under measured source bindings. Any source-floor, member-byte, extension-world, extension-run-time, packet-verifier, or bootstrap drift causes refusal.

The permanent product denominator is ten closed source files. Initial admission was additive; later repairs may modify only a nonempty subset of that denominator and may not delete or rename a member. Generated kits, packets, captures, controller bodies, confirmations, private decisions, operator identities, host identities, endpoints, credentials, prompts, completions, token text, network addresses, model bodies, and physical receipts remain outside the repository.

## Actors and separation

Nehanth Narendrula remains the publicly observed actor associated with the source observation that opened the supplier row. SwarmLLM remains one replaceable `OBSERVED_CANDIDATE` implementation beneath `axm/distributed-model-inference@1`. Neither the actor nor the product enters the executable profile, controller template, work-unit identity, terminal law, validator denominator, source-binding identity, or public projection. Removal or replacement of that supplier leaves the packet semantics unchanged.

The packet compiler is a source actor only. The two physical seats are independent evidence producers. The named human is a separate private decision actor who may confirm a completed evidence denominator but cannot be synthesized or inferred by the compiler. The independent verifier reconstructs both seat captures from their raw event ledgers and controller assertions. The measured bootstrap authenticates the verifier bytes that actually execute. None of these actors may grant supplier admission or operational authority.

## Mechanism

`build-kit` creates an external, non-repository run root containing an unpacked Manifest V3 Chromium-family extension, the exact admitted probe, two body-free controller templates, a named-human confirmation template, the frozen profile, all measured verification programs, a source-binding receipt, a deterministic kit manifest, and a private runbook. The extension contract requires the admitted probe to execute at `document_start` in the page `MAIN` world. The builder also materializes the same exact probe at `source/browser_distributed_inference_probe.js`, which is the dependency coordinate used by the standalone packet runtime documented in the runbook. Kit verification binds both copies to the admitted SHA-256 value, requires byte identity between them, verifies the complete `kitSourceBindings` denominator under `source/`, and refuses a missing, substituted, divergent, or extra runtime member. Continuous integration may build and verify this kit, but it does not launch a browser, contact an endpoint, download a model, form a peer connection, or perform inference.

Each completed private packet contains exactly two independently captured seat records. Every seat retains a raw probe export and a controller object privately. The compiler materializes each capture through the exact admitted browser-audition core, executes the admitted direct verifier from measured bytes, and binds the raw, controller, normalized capture, materialization receipt, observation decision, and admitted-verifier verdict into the packet evidence root.

Cross-seat identity does not depend on per-capture opaque member identifiers. Both controller objects must independently name the same two distinct SHA-256 physical-member evidence references, prove physical uniqueness, bind the same model-artifact denominator, observe selected topology, use ordered and reliable activation transport, complete the timing denominator, execute controlled member removal, establish output equivalence, report browser-visible network exposure, and retain an observer-bounded privacy declaration. One-seat substitution, seat replay, duplicate members, divergent member sets, late instrumentation, supplier-pinned work units, model disagreement, output disagreement, incomplete topology, unreliable transport, incomplete timing, incomplete removal, privacy overclaim, mixed source kinds, and private-data leakage terminate `HOLD` or `REFUSED`.

## Named-human boundary

A complete physical pair without a confirmation terminates `READY_FOR_NAMED_HUMAN`. The compiler does not create, infer, sign, or grant that decision. A separately supplied confirmation must be current, body-free, content-addressed, transaction-bound, authority-none, and bound to the packet evidence root, both seat capture digests, the two physical-member evidence references, and an external SHA-256 `actorEvidenceRef`.

The `actorEvidenceRef` is an evidence reference rather than a self-authenticating identity claim. A syntactically valid reference and a content-addressed confirmation do not prove which human acted, whether that person held the intended role, or whether the external evidence body is trustworthy. Those facts remain a private operator evidentiary obligation outside this source transaction. The packet verifier establishes only that the supplied confirmation satisfies the closed schema and binds the exact reconstructed transaction.

After the separate record is supplied, `assemble` may replace only derived packet outputs while preserving the raw seat inputs. Independent `verify` reconstruction must still pass before the route can reach `OBSERVED_ROUTE_CANDIDATE`. The resulting terminal admits only the observed route. It does not admit the supplier, qualify a physical Estate, or grant mission, command, targeting, engagement, effector, or weapons authority.

## Terminal and receipt law

The terminal denominator is closed:

```text
PREPARED_NOT_EXECUTED
READY_FOR_NAMED_HUMAN
OBSERVED_ROUTE_CANDIDATE
HOLD
REFUSED
```

An empty packet reaches `PREPARED_NOT_EXECUTED`. A complete physical pair awaiting its external decision reaches `READY_FOR_NAMED_HUMAN`. Synthetic fixtures may exercise `OBSERVED_ROUTE_CANDIDATE` only with `syntheticConformanceOnly: true`, `physicalExecutionObserved: false`, `actualSupplierQualified: false`, and `supplierAdmissionReceiptPresent: false`. A real private packet may reach the same route terminal only after two physical-private-local captures and the separately supplied confirmation pass independent reconstruction. Structural, source-authentication, unsafe-path, linked-coordinate, verifier-substitution, output-containment, and malformed-evidence failures terminate `REFUSED`. Complete but semantically insufficient evidence terminates `HOLD`.

The private receipt graph includes exact source bindings, kit identity, per-seat raw and controller identities, normalized capture identities, materialization receipts, admitted-verifier verdicts, model and member denominators, packet evidence root, optional named-human confirmation identity, packet decision identity, direct verifier verdict, and measured-bootstrap verdict. The public projection contains only the closed body-free status keys. It excludes prompts, completions, token text, SDP, ICE addresses, device labels, raw URLs, model URLs, credentials, response bodies, hostnames, local paths, private evidence bodies, operator names, and endpoint identities.

## Independent reconstruction and custody

The direct packet verifier validates the full profile, reconstructs the source-binding identity, measures every bound source file by byte count and SHA-256, audits the packet tree, reconstructs each seat from immutable staged copies, executes the admitted verifier from measured bytes under an isolated interpreter, recomputes the materialization receipt, recomputes the packet decision, and compares the stored body-free public status with the authenticated reconstruction. It cannot assert bootstrap authentication.

The outer bootstrap performs bounded reads of the packet verifier, validates its exact byte count and SHA-256 binding, rejects linked source, packet, decision, or output coordinates, executes only the measured verifier bytes under an isolated interpreter, verifies stored-member binding, and then marks the outer verdict `bootstrapAuthenticated: true`. Verification and projection outputs must remain outside both the measured source tree and the packet root. Caller-relative coordinates are resolved before foreign-directory execution without allowing a cross-volume interpretation to alter the selected object.

## Qualification contract

The permanent workflow uses an explicit Ubuntu and Windows matrix over exact head and synthesized merge coordinates. Every coordinate materializes the ten product members from exact Git blobs, rematerializes the five admitted dependencies from the admitted commit, independently remeasures both sets, verifies the predecessor tree and all ten predecessor blob identities, requires every changed path to remain inside the closed ten-member source denominator and refuses deletion or rename, compiles the Python source, checks the admitted probe syntax, executes all eighty-two hostile witnesses, reconstructs all twenty-three fixture cases, builds and verifies the external kit, invokes the runbook's standalone `source/` assembly command against a fresh packet root on every matrix platform, assembles and independently verifies the synthetic two-seat route, exercises the physical pre-human stop, runs both the repository and generated-kit PowerShell command surfaces on Windows, and preserves body-free qualification receipts. On Windows, the exact LF dependency bytes may differ from checkout-smudged working-tree bytes while remaining identical to the admitted Git blobs. The coordinate receipt therefore excludes those five already measured dependencies from the unrelated-residue query only after their bytes, lengths, and SHA-256 values independently match the admitted source binding.

The comparison job requires four receipt sets for pull requests and two for push or manual qualification. Profile, fixture, campaign, source-set, kit, synthetic decision, direct verdict, bootstrap verdict, public projection, and pre-human decision objects must be byte-identical across operating system and coordinate. The workflow is read-only and cannot push a branch, retain a generated packet, or promote any physical or supplier claim.

## Evidence ledger and claim boundary

The evidence tier is source-qualified physical-audition preparation with synthetic hostile reconstruction and a directly executed standalone-kit path. The venue is issues `BigBirdReturns/ai-execution-audit#98` and `BigBirdReturns/ai-execution-audit#106`, the exact ten-file product, and its hosted exact-coordinate qualification. The target is the unresolved private two-seat measurement transaction above the admitted observation membrane. The upside is a deterministic external kit and independently reconstructable private packet whose public projection is body-free and supplier-neutral. The downside is that browser instrumentation remains limited to browser-visible facts, while named-human provenance remains external to the compiler. The failure mode is `HOLD` or `REFUSED`, with no supplier admission and no physical, mission, command, targeting, engagement, effector, or weapons authority.

```text
source-qualified packet mechanism constructed: true
external run kit deterministically buildable:   true
synthetic route semantics exercised:            true
browser launched by source qualification:       false
external endpoint contacted by qualification:   false
model downloaded by qualification:              false
peer connection formed by qualification:        false
inference executed by qualification:            false
actual physical audition completed:             false
actual named-human decision supplied:            false
actual SwarmLLM qualified:                       false
actual supplier qualified:                      false
supplier admission receipt present:             false
physical Estate qualified:                      false
mission authority:                              none
command authority:                              none
targeting authority:                            none
engagement authority:                           none
effector authority:                             none
weapons authority:                              none
```

## Control question

Can two independently operated physical browser seats produce one exact, independently reconstructable, body-free route disposition while making the supplier, product interface, aggregate-memory display, privacy marketing, operator identity, compiler, stored decision, and verifier incapable of qualifying themselves or widening authority?
