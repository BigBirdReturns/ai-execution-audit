"""Runtime adapter stubs.

Adapters let others wrap real execution stacks (SLURM, containers, vendor SDKs) and
run them through the same audit checks.

These are stubs. They intentionally avoid any vendor SDK or cluster dependency.
"""